# Упрощенная версия - Минимальный тест

## Изменения

Это упрощенная версия приложения с минимальной функциональностью:

### ✅ Что осталось:

1. **Тест логика** - прохождение теста с вопросами
2. **Таймер** - фронтенд таймер (20 минут)
3. **Результаты** - отображение баллов и процента после завершения
4. **Статические вопросы** - вопросы хранятся в массиве в `src/services/api.ts`

### ❌ Что удалено:

1. ❌ Авторизация (auth)
2. ❌ Admin панель
3. ❌ Роли пользователей
4. ❌ Бекенд логика
5. ❌ Supabase RPC функции
6. ❌ Проверки прав доступа

### 📝 Структура:

```
src/
├── pages/
│   ├── Home.tsx        - Главная страница с кнопкой "Начать тест"
│   ├── Test.tsx        - Страница теста с вопросами и таймером
│   └── Result.tsx      - Страница с результатами (баллы, процент)
├── components/
│   ├── Question.tsx    - Компонент вопроса (logic/motivation)
│   ├── Timer.tsx       - Компонент таймера
│   ├── ProgressBar.tsx - Прогресс бар для результатов
│   └── LanguageSwitcher.tsx - Переключатель языка
├── services/
│   ├── api.ts          - Статические вопросы и логика расчета
│   └── supabaseClient.ts - Supabase клиент (опциональный, с fallback)
└── App.tsx             - Роутинг (Home → Test → Result)
```

### 🔧 Как работает:

1. **Вопросы** хранятся статически в `src/services/api.ts` в массиве `QUESTIONS`
2. **Таймер** работает только на фронтенде, автоматически завершает тест при истечении времени
3. **Результаты** рассчитываются локально:
   - Считаются только логические вопросы (logic)
   - Мотивационные вопросы (motivation) не оцениваются
   - Процент = (правильные ответы / общее количество logic вопросов) × 100
4. **Supabase** - опциональный, если не настроен, используется mock функция

### 🎨 UI:

- Чистый и минималистичный дизайн
- Поддержка русского и кыргызского языков
- Адаптивная верстка

### 📦 Запуск:

```bash
npm install
npm run dev
```

Приложение будет работать даже без настройки Supabase - все функции работают локально.
